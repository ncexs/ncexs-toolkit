name: 🧱 Build ncexs Toolkit

on:
  push:
    branches:
      - main
    paths:
      - "*.ps1"

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 🧩 Checkout Repository
        uses: actions/checkout@v4

      - name: ⚙️ Run PowerShell Script (Syntax Check)
        shell: pwsh
        run: |
          Write-Host "=== Running ncexs Toolkit Build ==="
          $files = Get-ChildItem -Path . -Filter "*.ps1"
          foreach ($f in $files) {
            Write-Host "🔍 Checking: $($f.Name)"
            try {
              pwsh -NoLogo -NoProfile -Command "& { . .\$($f.Name); Write-Host '✅ Syntax OK' }"
            } catch {
              Write-Host "❌ Error in $($f.Name): $($_.Exception.Message)"
              exit 1
            }
          }

      - name: 🧾 Save Build Log
        shell: pwsh
        run: |
          $logPath = "build-log.txt"
          "Build completed at $(Get-Date)" | Out-File $logPath
          "Checked files:" | Out-File $logPath -Append
          Get-ChildItem -Filter "*.ps1" | ForEach-Object { $_.Name } | Out-File $logPath -Append

      - name: 📤 Upload Build Log
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build-log.txt
