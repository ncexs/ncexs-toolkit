name: 🧱 Build ncexs Toolkit

on:
  push:
    branches:
      - main
    paths:
      - "*.ps1"

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 🧩 Checkout Repository
        uses: actions/checkout@v4

      - name: ⚙️ Static Syntax Check (no execution)
        shell: pwsh
        run: |
          Write-Host "=== Checking PowerShell syntax safely ==="
          $ErrorActionPreference = "Stop"
          $files = Get-ChildItem -Path . -Filter "*.ps1" -Recurse
          foreach ($f in $files) {
            Write-Host "🔍 Parsing: $($f.FullName)"
            try {
              [System.Management.Automation.PSParser]::Tokenize((Get-Content $f.FullName -Raw), [ref]$null) | Out-Null
              Write-Host "✅ OK - $($f.Name)"
            } catch {
              Write-Host "❌ Syntax error in $($f.Name): $($_.Exception.Message)"
              exit 1
            }
          }

      - name: 🧾 Save Build Log
        shell: pwsh
        run: |
          $logPath = "build-log.txt"
          "Build completed at $(Get-Date)" | Out-File $logPath
          "Checked files:" | Out-File $logPath -Append
          Get-ChildItem -Filter "*.ps1" | ForEach-Object { $_.Name } | Out-File $logPath -Append

      - name: 📤 Upload Build Log
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build-log.txt
